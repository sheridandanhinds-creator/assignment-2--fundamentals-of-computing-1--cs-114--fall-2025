public class TheTwelveDaysOfChristmas {
  public static void main(String[] args) {
    String suffix = " ";
    for(int day = 1; day < 12; day++){
      switch (day)
      {
      case 1: 
      suffix = "st";
      break;

      case 2:
      suffix = "nd";
      break;

      case 3:
      suffix = "rd";
      break;

      default:
      suffix = "th";
    }

     switch (day)
      {
      case Value1
      //Statement to execute if Expression == Value
      break;

      case Value2
      //Statement to execute if Expression == Value
      break;

      case Value3
      //Statement to execute if Expression == Value
      break;

      case Value4
      //Statement to execute if Expression == Value
      break;

      case Value5
      //Statement to execute if Expression == Value
      break;

      case Value6
      //Statement to execute if Expression == Value
      break;

      case Value7
      //Statement to execute if Expression == Value
      break;

      case Value8
      //Statement to execute if Expression == Value
      break;

      case Value9
      //Statement to execute if Expression == Value
      break;

      case Value10
      //Statement to execute if Expression == Value
      break;

      case Value11
      //Statement to execute if Expression == Value
      break;

      default:
      //Statement to execute if no case matches
      break;
      }
    }
  }
}